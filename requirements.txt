# These libraries are already available in your environment
# Flask, SQLAlchemy, requests, glob2 are included in your list
# No additional installation needed for the core functionality

# Production server (install if needed)
# waitress==2.1.2

# Alternative production server (install if needed)
# gunicorn==21.2.0 